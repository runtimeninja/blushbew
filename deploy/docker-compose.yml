services:
  db:
    image: postgres:16
    container_name: blushbew-db
    environment:
      POSTGRES_USER: blushbew
      POSTGRES_PASSWORD: blushbew
      POSTGRES_DB: blushbew
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
